# Dividend Reminder 0.5 (分红再投资智能提醒系统 PRD)

> **版本变更说明：** 基于 0.4 版讨论后，整合 7 项架构决策，修正港股税率逻辑，补充 ICS 日历实现方案，明确原型阶段技术选型。

---

## 📌 需求背景与核心目标

我是中国 A 股与港股（港股通）的长期价值投资者。持有的股票会定期发放现金红利。为实现**复利滚雪球（DRIP）**，需要在分红现金**真正划入证券账户的第一时间**收到通知，并将资金手动执行"分红再投资"。

本系统旨在构建一套自动化监控与多通道提醒工具，解决分红信息获取不及时、现金到账日期模糊以及跨市场（A 股/港股）红利税与汇率计算的痛点。

---

## 🏗️ 技术选型总览（已决策）

| 决策项 | 选择 | 说明 |
|--------|------|------|
| **数据源** | AkShare + 新浪财经 双源对比 | 日期不一致 → 人工介入；仅金额不一致 → 随机取一个 |
| **UI 框架** | Streamlit | 原型阶段，手机/电脑浏览器可访问 |
| **定时引擎** | GitHub Actions | 免费，接受 ±5 分钟延迟 |
| **主力提醒** | 企业微信 Webhook | 即时推送，微信内直达 |
| **备份提醒** | ICS 日历文件 | 提前可见 + 派息日当天系统级闹钟兜底 |
| **港股税率** | 自动区分 H 股(10%) / 非 H 股(20%) | 接受一定误差 |
| **A 股持股时间** | 默认 >1 年(免稅) | 用户可手动改为 <1 月(20%) 或 1 月~1 年(10%) |
| **数据缺失** | 日期缺失 → 阻塞 + 要求手动输入 | 金额缺失 → 标记"待查"，程序继续 |

---

## 🛠️ 一、 软件输入与持仓管理

系统需提供 Streamlit 可视化界面，支持对监控标的进行 CRUD 操作：

### 1. 标的录入与校验
- 用户输入股票代码（如 `600900` / `00700`）或股票名称（如 `长江电力` / `腾讯控股`）
- 系统调用 AkShare + 新浪财经接口校验代码有效性，补全市场前缀（如 `600900.SH` / `00700.HK`）及公司全称
- 校验失败时明确提示用户，不写入数据库

### 2. 持仓股数与红利税档位
- 用户为每只股票设定当前持有股数（`shares_held`）
- **A 股红利税档位**：默认 `>1 年`（免稅），可手动切换至 `<1 月 (20%)` 或 `1 月 ~ 1 年 (10%)`
- **港股通红利税**：系统自动根据上市公司注册地判断——H 股 10%，非 H 股 20%（接受一定误差，不做手动切换）
- 原型阶段不做 FIFO 先进先出跟踪

### 3. 持仓列表看板（Dashboard）
- 表格展示：股票代码、股票名称、持有股数、红利税档位、分红状态、预估净到账金额
- 支持编辑、删除、手动刷新单只股票状态
- 数据存储：**SQLite 数据库**

---

## 📊 二、 分红数据引擎与金融计算逻辑

### 1. 日期生命周期与核心锚点

系统从数据源中解析以下 4 个日期，锁定**派息到账日为核心提醒锚点**：

| 节点 | 说明 |
|------|------|
| 预案公告日 | 上市公司发布分红方案预案 |
| 股权登记日 | 确定享有分红权益的股东名单 |
| 除权除息日 | 股价开盘扣减除息 |
| **派息到账日 🎯** | 现金划入证券账户。所有提醒**强行绑定在此日期当天 09:15** 触发 |

### 2. 数据获取策略（双源对比）

```
         ┌───────────┐     ┌────────────┐
         │  AkShare  │     │  新浪财经   │
         └─────┬─────┘     └──────┬─────┘
               │                  │
         ┌─────┴────────┬─────────┴─────┐
         │              │               │
    日期一致 ✓      日期不一致 ✗     仅一个源有数据
         │              │               │
         ▼              ▼               ▼
    自动处理       标记"需人工确认"    使用可用数据
                  暂停自动提醒       标记来源
         │
    金额一致？─── 一致 → 随意取
         │
        不一致 → 随机选一个（不阻塞）
```

**缺失处理规则：**
- **日期缺失** → 标记"需手动输入"，阻塞该股票的所有提醒，企业微信通知用户介入
- **金额缺失**（罕见） → 标记"金额待查"，程序继续运行，日历和提醒中注明

### 3. 手动数据入口
- 所有分红字段（4 个日期 + 每股派息）在 Streamlit 界面上均可手动编辑
- 手动修改后的数据不再被自动刷新覆盖，除非用户主动点击"重新获取"

### 4. 税后净分红与汇率计算引擎

#### 规则 A：A 股差别化红利税
| 持股时间 | 税率 | 系统行为 |
|----------|------|----------|
| > 1 年 | 0% | **默认值** |
| 1 个月 ~ 1 年 | 10% | 用户手动切换 |
| < 1 个月 | 20% | 用户手动切换 |

**计算公式：**
$$\text{预估 A 股净到账 (RMB)} = \text{持股数} \times \text{每股税前派息} \times (1 - \text{税率})$$

#### 规则 B：港股通红利税与汇率

| 股票类型 | 注册地 | 税率 | 举例 |
|----------|--------|------|------|
| H 股 | 中国内地 | **10%** | 工商银行 H 股、中石油 H 股 |
| 非 H 股（红筹/香港本地） | 海外/香港 | **20%** | 中国移动、汇丰控股 |

系统自动从数据源判断上市公司注册地以确定税率，接受一定误差。

**计算公式：**
$$\text{预估港股通净到账 (RMB)} = \text{持股数} \times \text{每股派息(HKD)} \times (1 - \text{税率}) \times \text{HKD/CNY 汇率}$$

- 汇率取当日新浪财经或央行中间价，不做毫秒级实时汇率

---

## ⚙️ 三、 智能轮询调度策略

通过 GitHub Actions 定时触发（cron），结合状态机动态调整频率：

| 状态 | 财报季（3–4 月 / 8–9 月） | 非财报季 |
|------|---------------------------|----------|
| 未公布分红 | 每日 1 次 | 每周 1 次 |
| 已预告但无派息日 | 每日 1 次 | 每日 1 次 |
| 已锁定派息日 | 停止轮询，写入定时提醒 | 同左 |

- GitHub Actions 最小粒度为 5 分钟，定时 cron 形式
- 接受 ±5 分钟的提醒延迟

---

## 🔔 四、 多通道智能提醒系统

### 1. 主力通道：企业微信 Webhook

- **实现方式：** 群机器人 Webhook 发送 Markdown/文本消息
- **前置条件：** 用户预先创建企业微信群机器人，获取 Webhook URL，填入系统配置
- **消息内容：** 股票名称、派息到账日、每股派息、预估净到账金额（税后/汇率后）、再投资建议提醒
- **触发时间：** 派息日当天 09:15（GitHub Actions 定时触发，误差 ±5 分钟）
- **安全规范：** 仅使用 Webhook URL，不索要/存储用户的微信账号、密码或登录凭证

### 2. 备份通道：ICS 日历文件

```
┌─ GitHub Actions ──┐     ┌─ GitHub 仓库 ──┐     ┌─ iPhone 日历 ──┐
│ 每次运行后          │     │                │     │                 │
│ 重新生成 .ics 文件  │ ──→ │ output/        │ ──→ │ 日历 APP        │
│                    │     │ dividend.ics   │     │ 定期后台拉取     │
└────────────────────┘     └────────────────┘     └─────────────────┘
                              ↑ 用户一次性订阅
                              ↑ Raw URL 长期有效
```

- **订阅地址：** `https://raw.githubusercontent.com/<user>/<repo>/main/output/dividend.ics`
- **提醒策略：**
  - 派息日前 1 天 20:00 → 提前提醒（日历通知）
  - 派息日当天 09:15 → 系统级闹钟（兜底）
- **局限性（需理解）：**
  - ICS 不是实时推送，依赖手机后台刷新（可能延迟数小时）
  - 精准即时通知靠企业微信
  - 若派息日变动，`.ics` 文件需重新生成，手机端自动拉取更新
- **生成方式：** Python `ics` 或 `icalendar` 库生成标准 iCalendar 格式文件

### 3. 提醒通道对比总结

| | 企业微信 | ICS 日历 |
|------|----------|----------|
| 触达方式 | 微信消息即时推送 | 日历 APP 系统通知 |
| 送达时间 | 几乎实时 | 取决于手机刷新周期 |
| 可靠性 | 依赖微信服务 | 苹果/安卓系统级 |
| 提前提醒 | 不支持 | ✅ 可设 1 天前 |
| 准点响铃 | 消息通知 | ✅ 系统闹钟 |
| 角色定位 | **主力** | **备份兜底** |

---

## 🖥️ 五、 系统架构

### 原型阶段架构（0.5 版）

```
project/
├── .github/workflows/
│   └── scheduler.yml          # GitHub Actions 定时触发
├── src/
│   ├── data_sources/
│   │   ├── akshare_fetcher.py   # AkShare 数据获取
│   │   └── sina_fetcher.py      # 新浪财经数据获取
│   ├── engine/
│   │   ├── dividend_engine.py   # 双源对比 + 分歧处理
│   │   ├── tax_calculator.py    # A股 / 港股通 税后计算
│   │   └── poll_scheduler.py    # 智能轮询状态机
│   ├── reminders/
│   │   ├── wechat_webhook.py    # 企业微信推送
│   │   └── ics_generator.py     # ICS 日历文件生成
│   ├── db/
│   │   └── database.py          # SQLite 持仓 & 分红日历
│   ├── ui/
│   │   └── app.py               # Streamlit 管理界面
│   └── main.py                  # CLI 入口（GitHub Actions 调用）
├── output/
│   └── dividend.ics             # 生成的日历文件（供手机订阅）
├── config/
│   └── settings.yaml            # Webhook URL、用户配置等
├── requirements.txt
└── README.md
```

### 数据流

```
用户持仓(SQLite) → 轮询调度器 → AkShare + 新浪财经
                                    ↓
                             双源对比仲裁引擎
                                    ↓
                           ┌────────┴────────┐
                         一致                不一致(日期)
                           ↓                    ↓
                     计算税后净额          通知用户确认
                           ↓
                    ┌──────┴──────┐
                    ↓              ↓
              企业微信推送    ICS 文件生成
                    ↓              ↓
              微信消息通知    GitHub Raw URL
                                ↓
                          手机日历订阅
```

### 未来演进（≥0.6 版）

- 定时引擎迁移至 VPS → 取消 GitHub Actions 延迟
- Streamlit 包裹 FastAPI → 为手机 APP 提供 REST 接口
- iOS / Android APP（SwiftUI / Flutter）替代 Web 界面
- 多账户、FIFO 先进先出精确税率跟踪

---

## 🔒 六、 安全与鉴权规范

- 企业微信仅通过 **Webhook URL** 发送消息（Token-based 无状态协议）
- 系统配置与代码**严禁**索要、存储或传输用户的微信账号、登录密码、支付密码、手机验证码
- 所有敏感配置（Webhook URL、API Token）存储在本地 `config/settings.yaml`，不提交至公开仓库（通过 `.gitignore` 排除或使用 GitHub Secrets）

---

## 📋 七、 待实现功能清单

### 原型阶段（0.5 版）

- [ ] SQLite 数据库设计与初始化
- [ ] 持仓 CRUD（Streamlit 界面）
- [ ] AkShare + 新浪财经双源数据获取
- [ ] 双源日期对比仲裁逻辑
- [ ] A 股税后计算 + 港股通税后计算（含汇率）
- [ ] 智能轮询状态机（财报季/非财报季判断）
- [ ] 企业微信 Webhook 推送
- [ ] ICS 日历文件生成与更新
- [ ] GitHub Actions 定时调度配置
- [ ] 手动编辑分红数据入口

### 后续版本

- [ ] FastAPI REST 接口层
- [ ] iOS / Android App
- [ ] 多账户支持
- [ ] FIFO 精确持股时间跟踪
- [ ] 推送渠道扩展（Telegram / Bark / PushDeer）

---

> **文档版本：** 0.5 | **日期：** 2026-07-30 | **状态：** 决策已完成，待开发原型
