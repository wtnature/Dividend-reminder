# Dividend Reminder 0.2 (分红再投资智能提醒系统 PRD)

## 📌 需求背景与核心目标

我是中国 A 股与港股（港股通）的长期价值投资者。我持有的股票会定期发放现金红利。为了实现**复利滚雪球（Dividend Reinvestment Plan, DRIP）**，我需要在分红现金**真正划入证券账户的第一时间**收到通知，并将这笔资金手动或半自动地执行“分红再投资”。

本系统旨在构建一套自动化监控与多通道提醒工具，解决分红信息获取不及时、现金到账日期模糊以及跨市场（A股/港股）红利税与汇率计算复杂的痛点。

---

## 🛠️ 一、 软件输入与持仓管理需求

系统需提供简洁可视化的持仓管理界面（或配置文件接口），支持对监控标的进行 CRUD 操作：

1. **标的录入与校验（Input Validation）**：
   * 用户输入股票代码（如 `600900` / `00700`）或股票名称（如 `长江电力` / `腾讯控股`）。
   * 系统必须自动调用接口校验输入的正确性，补全市场前缀（如 `600900.SH` / `00700.HK`）及公司全称，防范无效代码。
2. **持仓股数与成本管理**：
   * 用户可为每只股票设定或修改当前持有股数（`shares_held`）。
   * （可选）支持输入持仓时间，用于后续 A 股差别化红利税计算。
3. **持仓列表看板（Dashboard UI）**：
   * 呈现所有已添加的股票列表，直观展示：股票代码、股票名称、持有股数、分红状态（未公布 / 已预告 / 已派息）、预估净到账金额。
   * 支持对列表中股票的编辑、删除以及一键手动刷新状态。

---

## 📊 二、 分红数据引擎与金融计算逻辑

系统必须区分中国资本市场分红的 4 个法定节点，并建立标准化的数据解析与算账引擎：

### 1. 日期生命周期与主锚点设定

系统从官方公告/API 中抓取以下 4 个日期，并锁定**派息日为核心提醒锚点**：
* **预案公告日**：上市企业发布分红方案预案。
* **股权登记日（Record Date）**：确定享受分红权益的股东名单。
* **除权除息日（Ex-Dividend Date）**：股价进行开盘扣减除息。
* **派息到账日（Payment Date，核心主锚点）**：真金白银划入证券账户的日子。所有“再投资提醒”**强行绑定在此日期当天 09:15（开盘前）**触发。

### 2. 税后净分红与汇率转换计算引擎

系统输出的“分红总额”不得采用简单的 $\text{持仓股数} \times \text{每股派息}$，必须经过以下税率与汇率引擎清洗：

#### 规则 A：A 股差别化红利税引擎
* **持股时间 $> 1$ 年**：免征红利税（税率 $0\%$）。
* **持股时间 $1$ 个月 $\sim 1$ 年**：预扣税率 $10\%$。
* **持股时间 $< 1$ 个月**：预扣税率 $20\%$。
* **默认计算公式**：
  $$\text{预估 A 股净到账 (RMB)} = \text{持股数} \times (\text{每股税前派息} \times (1 - \text{预估税率}))$$

#### 规则 B：港股通（南向资金）红利税与汇率引擎
* 国内个人投资者通过港股通买入港股/H 股，由中国结算统一代扣代缴 **$20\%$ 个人所得税**。
* **默认计算公式**：
  $$\text{预估港股通净到账 (RMB)} = \text{持股数} \times \text{每股派息(HKD)} \times (1 - 20\%) \times \text{实时 HKD/CNY 卖出汇率}$$

---

## ⚙️ 三、 智能查询与 API 降频策略

为了防止频繁请求公网数据源导致 IP 被封禁或触发免费 API 配额上限，系统需引入**动态智能轮询（Smart Polling）**：

1. **状态机轮询逻辑**：
   * **未公布状态**：处于财报季（年报 3–4 月，半年报 8–9 月）时，系统设定为**每日静默查询 1 次**；非财报季降低为**每周查询 1 次**。
   * **已预告但无派息日**：若企业公布了分红预案但尚未指定派息日，系统提升频次为**每日 1 次**，直至抓取到明确的 `Payment Date`。
   * **已锁定派息日**：暂停该股票的日常 API 轮询，直接在数据库中写入定时提醒任务。
2. **数据源降级与备用机制（Fallback）**：
   * 主接口：开源/免费金融数据 API（如 AkShare / TuShare / 新浪财经 API）。
   * 备用接口：上交所、深交所、港交所官方公告爬虫兜底。

---

## 🔔 四、 多通道智能提醒系统与安全鉴权规范

系统需提供以下稳定、无被封风险的提醒服务方案，用户可自主配置：

### 1. 📢 提醒通道选型

* **选项 A：企业微信应用 / Webhook 通道（推荐⭐）**
  * 实现路径：通过企业微信自建应用或群机器人 Webhook，消息经由“微信插件”直接无缝送达用户的日常微信消息列表中。
  * 优势：100% 官方安全通道、完全免费、零额度限制、极高稳定性。
* **选项 B：Server 酱 / PushDeer 第三方推送通道**
  * 实现路径：基于第三方平台的 `SendKey` API，将卡片消息推送到公众号。
* **选项 C：手机原生日历与响铃提醒通道**
  * 实现路径：系统生成标准 `.ics`（iCalendar）日历文件或 CalDAV 订阅服务链接，手机订阅后在派息日 09:15 触发系统级响铃提醒。

### 2. 🔒 安全与鉴权规范（强制合规红线）

* **无状态 Token 鉴权**：微信推送模块必须严格基于 **企业微信 Webhook / Secret** 或 **Server 酱 SendKey** 等 Token-based 无状态协议实现。
* **绝对账号隔离**：系统配置与代码层**严禁以任何形式索要、存储或传输用户的个人微信账号、微信登录密码、支付密码、手机验证码或登录 Session 凭证**，杜绝任何封号与隐私泄露隐患。

---

## 🖥️ 五、 系统架构与部署形态规范

为了保证监控与提醒能 7x24 小时稳定运行（不依赖个人电脑开机）：

1. **后端运行载体**：
   * 优先采用 **轻量级云服务器 (VPS)**、**腾讯云/阿里云 Serverless 函数** 或 **GitHub Actions 定时任务** 驱动。
2. **前端交互界面**：
   * 采用极简 Web 界面（如 Python Streamlit / Vue.js）或桌面 GUI（如 PyQt / CustomTkinter），支持手机和电脑浏览器访问管理。
3. **数据持久化**：
   * 持仓列表与派息日历存储于本地 SQLite 数据库或轻量级 JSON 配置文件中。